# 待办清单

按优先级排列。做完的会移到「已完成」，不会直接删掉。

## 高优先级

1. **移动端适配**  
   当前在 375px 宽度下，导航会挤到第二行。需要改成横向可滑动的形式。

2. **文档搜索**  
   `docs.json` 条目多了以后，光靠滚动不好找。计划加一个输入框，匹配标题与描述。

3. **复制计数去重**  
   现在同一次选中、反复复制会重复计数。需要加一个 3 秒内同文档只记一次的节流。

## 中优先级

- TOOL 页面支持分类标签
- DROP 页面显示文件下载次数（需要后端）
- 文章正文支持脚注
- 代码块加复制按钮

## 低优先级

- 浅色模式（`data-theme="light"` 的变量已经写好了，但没接开关）
- 键盘快捷键：`H` 回首页，`D` 进文档
- 星光在低性能设备上自动降低密度

## 已完成

- [x] 星光质量模型与环绕分层
- [x] 白块擦除过渡
- [x] L 形角标适配文字宽度
- [x] TOOL 读取 `tools/tools.json`
- [x] DROP 读取 `drops/drops.json`
- [x] 文档按日期分组与滚动折叠

---

## 一些被否掉的想法

> **做一个评论系统**  
> 静态站点没有后端，接入第三方评论会引入外部依赖和隐私问题。放弃。

> **把每篇文章做成单独页面**  
> 会让路由和过渡逻辑复杂一倍，收益不明显。保持单页应用形态。

> **给星星加拖尾**  
> 试过，视觉上太吵，尤其在暗色背景上会糊成一片。放弃。